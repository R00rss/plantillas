import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import MultiFile from './components/files/MultiFile/MultiFile'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <MultiFile />
  </React.StrictMode>,
)
